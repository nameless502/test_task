export enum ErrorsStatusCodes {
    badRequest = 400,
    notAuthorized = 401,
    forbidden = 403,
    notFound = 404,
    conflict = 409,
    default = 500,
}

export enum SuccessStatusCodes {
    success = 200,
    created = 201,
    updated = 204,
}
